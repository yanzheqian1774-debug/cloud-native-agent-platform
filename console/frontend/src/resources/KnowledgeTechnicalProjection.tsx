import type { KnowledgeProjection } from "../api/knowledgeResources";
export function KnowledgeTechnicalProjection({projection}:{projection:KnowledgeProjection}){const value=projection.technicalProjection;return <section aria-label="Knowledge Technical View" className="agent-technical"><p className="eyebrow">Technical View · same canonical object</p><h3>Authority and derived index</h3><dl><dt>Knowledge identity</dt><dd className="technical-value">{String(value.knowledgeId)}</dd><dt>Scope</dt><dd className="technical-value">{String(value.namespace)} / {String(value.securityDomain)}</dd><dt>Published revision</dt><dd className="technical-value">{String(value.publishedRevisionId??"NOT_PUBLISHED")}</dd><dt>Active index snapshot</dt><dd className="technical-value">{String(value.activeIndexSnapshotId??"NOT_INDEXED")}</dd></dl><p>PostgreSQL owns lifecycle and identity. Qdrant contains derived vectors only.</p></section>}
