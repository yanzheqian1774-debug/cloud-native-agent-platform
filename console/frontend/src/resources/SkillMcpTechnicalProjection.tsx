import type { ResourceProjection } from "../api/skillMcpResources";
export function SkillMcpTechnicalProjection({projection}:{projection:ResourceProjection}){const technical=projection.technicalProjection as {resourceId:string;namespace:string;securityDomain:string;aggregateVersion:number;publishedRevisionId:string|null;revisionDigests:Array<{revisionId:string;digest:string;state:string}>;limitations:string[]};return <details className="agent-technical"><summary>Technical sibling projection</summary><dl><dt>Canonical identity</dt><dd>{technical.resourceId}</dd><dt>Scope</dt><dd>{technical.namespace} / {technical.securityDomain}</dd><dt>Aggregate version</dt><dd>{technical.aggregateVersion}</dd><dt>Published revision</dt><dd>{technical.publishedRevisionId??"Not published"}</dd></dl><ul>{technical.revisionDigests.map(item=><li key={item.revisionId}><strong>{item.state}</strong> <code>{item.digest}</code></li>)}</ul><p>{technical.limitations.join(" · ")}</p></details>}
