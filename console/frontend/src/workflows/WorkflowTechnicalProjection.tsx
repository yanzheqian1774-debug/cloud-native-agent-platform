import type { WorkflowProjection } from "../api/workflowDefinitions";
export function WorkflowTechnicalProjection({projection}:{projection:WorkflowProjection}){return <section className="technical-projection" aria-label="Workflow Technical projection"><h3>Technical projection</h3><dl>{Object.entries(projection.technicalProjection).map(([key,value])=><div key={key}><dt>{key}</dt><dd className="technical-value">{typeof value==="string"?value:JSON.stringify(value)}</dd></div>)}</dl><p>Definition and authoring Evidence only. Selected tasks and edges are context, not Run state. Kubernetes execution authority is unchanged; this projection cannot create or mutate Pods.</p></section>}
