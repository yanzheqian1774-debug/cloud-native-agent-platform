import { useEffect,useState } from "react";
import { compareWorkflowRevisions,type WorkflowDefinition } from "../api/workflowDefinitions";
export function WorkflowVersionComparison({definition}:{definition:WorkflowDefinition}){const [comparison,setComparison]=useState<Record<string,unknown>|null>(null);useEffect(()=>{if(definition.revisions.length<2)return;const [left,right]=definition.revisions.slice(-2);compareWorkflowRevisions(definition.workflowDefinitionId,left.revisionId,right.revisionId).then(setComparison).catch(()=>setComparison({state:"COMPARISON_UNAVAILABLE"}))},[definition]);return <section><h3>Version comparison</h3>{comparison?<pre className="technical-value">{JSON.stringify(comparison,null,2)}</pre>:<p>No successor comparison is available yet.</p>}</section>}
