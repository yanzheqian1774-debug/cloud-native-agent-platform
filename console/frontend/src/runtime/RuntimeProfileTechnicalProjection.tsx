import type { RuntimeProjection } from "../api/runtimeProfiles";
export function RuntimeProfileTechnicalProjection({projection}:{projection:RuntimeProjection}){return <section className="technical-projection" aria-label="Runtime Profile Technical projection"><h3>Technical projection</h3><dl>{Object.entries(projection.technicalProjection).map(([key,value])=><div key={key}><dt>{key}</dt><dd className="technical-value">{typeof value==="string"?value:JSON.stringify(value)}</dd></div>)}</dl><p>Profile definition only. Pod creation, exec, raw environment, logs, OpenClaw execution, routing and scaling are outside this module.</p></section>}
