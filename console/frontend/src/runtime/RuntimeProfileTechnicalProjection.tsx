import type {RuntimeProjection} from "../api/runtimeProfiles";
export function RuntimeProfileTechnicalProjection({projection}:{projection:RuntimeProjection}){return <section className="technical-projection" aria-label="Runtime Profile Technical projection"><h3>Technical projection</h3><dl>{Object.entries(projection.technicalProjection).map(([key,value])=><div key={key}><dt>{key}</dt><dd className="technical-value">{typeof value==="string"?value:JSON.stringify(value)}</dd></div>)}</dl><p>Profile definition only: declaration-only and Secret Reference-only. Model references are unverified. Pod creation, placement, execution, exec, raw environment, logs, OpenClaw-running status, routing and scaling are unsupported here.</p></section>}
