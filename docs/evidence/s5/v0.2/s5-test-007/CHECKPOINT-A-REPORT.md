# Checkpoint A Report

## Scope

S5-TEST-007 adds one integrated acceptance test and eight evidence artifacts.
Application, bridge, Package 7, existing tests, dependencies, lockfiles, CI,
architecture, governance and manifests remain unchanged.

## Baseline and ownership

- branch: `codex/s5-test-007-enhanced-golden-demo-acceptance`
- baseline: `cb3d7c8bb437961df1917be6bba26c91425fcc09`
- exact-main CI: `33294154890 / SUCCESS`
- predecessor PRs: `#86 / MERGED`; terminal-SSE fix `#87 / MERGED`
- Package 7 checksum inventory: ten entries
- integrated bridge inventory: seventeen paths

## Acceptance result

The bounded Native supplier-quality live path is accepted for internal
Technical Preview review. Genuine start/reset, canonical approval, published
role matching, authorized Knowledge retrieval and citations, Native placement
and execution, immutable correction successor, fresh approval, comparable
Outcome, append-only intervention, versioned feedback, ordered SSE and exact
Product/Technical identity equality are covered by the Package 8 test plus the
reused regression suites.

Failure and denial remain fail-closed. Live startup/network failure never
substitutes fixtures. Synthetic history is visibly separate from
`LIVE_EXECUTION`.

Direct Browser QA confirmed that terminal SSE closes normally without a
reconnect, the completed Product and Technical projections remain visible, and
the former false `JOURNEY_STREAM_UNAVAILABLE` state does not recur. The
backend-unavailable check displayed an explicit live error and no synthetic
substitution.

## Architecture impact

None. No public API, CRD, Kubernetes group, shared DTO, Canonical Graph,
persistence, Runtime lifecycle, dependency or architecture change is made.

## Validation results

- Package 8 plus predecessor/regression slices: `227 passed`, one existing
  Starlette/httpx deprecation warning.
- Focused Package 8 plus Console app ordering regression: `9 passed`, same
  warning.
- Conformance harness: `57 passed`.
- Full Ruff lint: passed; full Ruff format check: `160 files already formatted`.
- External frontend copy: ESLint passed; TypeScript and Vite production build
  passed (`72` modules transformed).
- `make check`: `948 passed`, one existing Starlette/httpx deprecation warning.
- All-files pre-commit: Ruff lint, Ruff format and pytest passed; before/after
  status and SHA-256 manifests were byte-identical.
- Post-hook `make check`: `948 passed`, same warning.
- Browser QA: Product/Technical, English/zh-CN and `1280×720`/`390×844`
  passed; successful-run browser warning/error log was empty; no horizontal
  overflow.
- `git diff --check`: passed.

## Terminal state

All nine authorized paths are uncommitted. Package 9, deployment, certification,
production readiness, release acceptance and downstream work remain unstarted.

## Checkpoint C terminal revalidation

Checkpoint C repeated the direct genuine Browser journey at the durable
baseline. Product and Technical each preserved terminal
`EXECUTION_SUCCEEDED`; `JOURNEY_STREAM_UNAVAILABLE` was absent, no terminal
reconnect occurred, browser warning/error logs were empty, and desktop/mobile
English/zh-CN presentation remained free of horizontal overflow.

Fresh validation repeated the focused matrix (`227 passed`), conformance (`57
passed`), frontend ESLint/TypeScript/Vite build, full Ruff checks and `make
check` (`948 passed`, one existing Starlette/httpx warning). Package 7's ten
checksums passed. S5-REL-041 is collision-free as the primary durable
integration candidate and S5-REL-042 is collision-free as fallback; neither
Session was started.
