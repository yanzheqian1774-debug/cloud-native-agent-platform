# Claim / Evidence Matrix

| Claim | Implementation evidence | Test or Browser evidence |
|---|---|---|
| Question-first; no automatic start | `App.tsx`, `QuestionToOutcomeJourney.tsx`, `supplierQualityDemo.ts` | Composer disabled until valid input; suggestion remained on entry page; focused no-auto tests passed. |
| Human question affects bounded semantics | `planning_generator.py`, `supplier_quality_demo.py` | Objective, scope, first decomposition item, and first task purpose contain the submitted question. |
| Draft is reviewable and non-executing | Draft projection in `supplier_quality_demo.py` | `PENDING`, `NOT_REQUESTED`, no execution identity/Evidence/Outcome, zero coordinator/provider effects. |
| Actual team/capability projection | Package role descriptors projected by `supplier_quality_demo.py` | Browser selected-task detail showed published role, Skill, `quality.read`, Knowledge Pack, and `native` Runtime. |
| DAG uses actual dependencies | `QuestionToOutcomeJourney.tsx` renders edges only when `task.dependencies.length > 0` | Browser found three nodes and two dependency edges; regression asserts dependency-derived labels. |
| Same DAG carries execution state | Terminal projection updates existing `projectedTasks` | Focused backend test and Browser both observed all three canonical nodes as `SUCCEEDED`. |
| Correction and exact approval | Existing correction/approval authorities reused in `supplier_quality_demo.py` | Browser observed changed digest; focused tests prove immutable predecessor and zero execution before explicit rerun. |
| Causal live event history | Existing journey stream authority reused | Focused test and Technical View show registered, authorized, started, then one terminal succeeded event. |
| Evidence-backed result and follow-up | Outcome/citations plus Evidence chain and `InterventionFeedback` | Browser observed seven chain stages, comparable Demo metric, citations disclosure, and valid intervention surface. |
| Product/Technical identity equality | One `LiveJourneyResponse` supplies sibling projections | Backend equality tests pass; Browser matched Product correction digest in Technical View. |
| Chinese-first and bilingual technical precision | `messages.ts`, Product journey, Technical panel | Browser verified Chinese default, English entry, bilingual labels, and exact raw values. |
| Responsive and accessible interaction | `app.css` mobile and focus rules | Browser measured no mobile horizontal overflow and visible keyboard focus; screenshots captured. |
| No synthetic/live blending | Explicit live branch and backend-issued journey API | Regression assertions and Browser Technical disclosure confirm live-only route. |
| Prominent current-journey team | `DigitalEmployeeDirectory.tsx` derives summary and cards from projected tasks | Browser observed two matched employees and the prominent Chinese team heading. |
| Bounded effective directory | Resource tables derive only from current task bindings | Scope/provenance labels are visible; no global catalog fetch or claim exists. |
| Canonical bidirectional mapping | URL presentation selection preserves object, task, revision, snapshot, and step | Browser selected `analyze-quality-exception`, highlighted it in Technical, and restored it in Product. |
| Supported cardinalities | Task bindings and reverse indexes in Product/Technical views | Tests prove 1:1 identity, 1:N Agent/tasks, N:1 task/Agent, and actual N:N task/Skill edges. |
| Structured Technical default | Seven `.technical-module` sections precede the diagnostic appendix | Browser confirmed all seven Chinese modules and secondary English terms. |
| JSON is an appendix | Bottom `details.raw-json` renders the exact authority objects | Browser confirmed `open=null`; source test asserts exact `JSON.stringify` payload. |
| Operationally safe stepper | Step buttons only scroll when `index <= stage` | Browser confirmed future execution/result steps disabled before approval. |
| Resource taxonomy is explicit | Directory tables separate Business Capability, Skill, MCP, Knowledge, Runtime, and Agent Definition | Focused tests assert category and semantic boundaries; no Model category is fabricated. |
| Agent Definition is not execution identity | Runtime scene shows the binding and explicit no-persistent-instance disclosure | Browser observed exact Definition ID and distinct `execution:…` Platform Execution Identity. |
| Runtime and Placement are authoritative | Runtime comes from task binding; placement and state come from the shared identity/revision | Browser observed `native`, exact placement decision, `PLACED`, and `APPROVED`. |
| Timeline uses only real events | Runtime scene renders received SSE envelopes | Browser observed registered, authorized, started, and succeeded in Chinese with four expandable raw records. |
| Missing facts are honest | Provider, telemetry, absent predecessor/time fields use the required unavailable language | Source/security tests exclude environment access, endpoints, paths, CPU/GPU, and invented Provider claims. |
| Status meaning is not color-only | Product directory, DAG, and Technical status components combine a semantic family with an icon and Chinese text | Focused accessibility regression asserts all eight families; Browser observed the labelled eight-family legend and actual execution/approval/placement badges. |
| Resource types remain distinct | Technical modules independently render Business Capabilities, Skills, and MCP Servers/Capabilities/Operations; Knowledge explains judgment provenance | Focused taxonomy tests assert the separate modules and that Knowledge is not executable Skill. |
| Visual hierarchy preserves technical precision | Bounded typography rules distinguish page, explanation, section, object, body, label, English, and raw-value roles | Frontend build passed; Browser responsive captures show readable desktop/mobile hierarchy, exact selectable monospace values, no overflow, and collapsed Raw JSON. |
| Local Demo access uses existing trusted authority | Backend startup supplies the existing server-owned principal, tenant, security domain, and materialized Package 7 root | Fresh Browser opens the unsubmitted composer and completes the full journey; missing/wrong scoped principals remain denied before all downstream call counters. |
| Product denial is understandable without hiding technical truth | `QuestionToOutcomeJourney.tsx` renders Chinese primary guidance and a collapsed Technical Details disclosure | Real denied Browser run showed no journey, no raw enum as dominant content, expandable exact reason code, and no mobile overflow. |
| Workspace-first navigation is coherent | `App.tsx` routes `/` to `/workspace` and defines the five Demo destinations | Direct Browser entry resolved to `/workspace`; all five navigation links and honest empty states were visible without manual URL construction. |
| Product and Technical are contextual projections | Product/Technical page switches preserve the full query string and breadcrumbs restore journey context | Browser opened the exact execution object, exercised six Technical tabs, and returned to `step=team` with the same task selected. |
| Directories use master-detail interaction | Employee, resource, and Runtime pages keep a selectable master list and one selected detail context | Desktop and `390×844` Browser QA selected the Supplier Quality Analyst; no horizontal overflow occurred. |
| Relationships are semantic and authority-bound | `RelationshipGraph` derives nodes and named edges only from the live revision projection | Browser displayed task, Agent Definition, Capability, Skill, MCP, Knowledge, Runtime, Evidence, and Outcome with action-labelled edges. |
| Future journey steps explain prerequisites | `review()` reports approval/execution prerequisites while lifecycle mutations remain in correction/approval handlers | Source contract test passed; future buttons use `aria-disabled` semantics without silent inert controls. |
| Technical content is progressively disclosed | Six contextual tabs expose relationships, workflow, execution scene, events, Evidence, and raw data | Browser confirmed selected-object and Runtime panels plus Raw JSON on demand; default page no longer expands all modules. |
| Approval remains complete after the decision | Product approval projection renders summary, resources, risks, four state families, unavailable facts, history/diff, and collapsed identity in every state | Browser verified all pending fields/actions, then verified completed `已批准` with decision actions absent. |
| State projections cannot contradict one another | Approval, execution authorization/task state, journey state, and placement are separately labelled from authoritative revision fields | Focused source regressions cover PENDING, APPROVED, REJECTED, RUNNING/EXECUTING, SUCCEEDED, and FAILED branches; Browser observed pending then successful terminal projection. |
| Journey progress is independent of inspection | Nine-step current progress and seven-section viewed position are separately labelled | Browser inspected historical approval/execution sections while current progress remained terminal. |
| Workflow is business-readable before identities | Workflow graph derives task order, employees, approval and execution boundaries from projected tasks and revision state | Browser observed start-to-end sequential graph; exact digests/snapshots were collapsed. |
| Evidence explains business meaning and provenance | Product and Technical render the seven-stage directed relationship and proof/finding/conclusion/source explanations | Browser verified Knowledge, deterministic analysis, Human approval, Runtime execution, Evidence, and Outcome linkage. |
| Employee responsibility is compact and separated | Employee projections use role-specific responsibility and distinct Capability/Skill/MCP/Knowledge/Runtime/Human fields | Mobile Browser confirmed no full question in responsibility and a readable semantic object chain. |
